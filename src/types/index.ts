export interface VideoConfig {
  id: number;
  videoUrl: string;
  thumbnailUrl: string;
}

export interface ApplicationFormData {
  name: string;
  phone: string;
  email: string;
  message: string;
}